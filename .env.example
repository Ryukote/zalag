# Database Configuration
DB_PASSWORD=postgres123

# JWT Configuration
JWT_SECRET=SuperTajniJWTKljuc123!Change_This_In_Production
JWT_ISSUER=zalagaonica.hr
JWT_AUDIENCE=zalagaonica-app

# Fiskalizacija Configuration (Croatian Fiscalization)
FISKALIZACIJA_BASE_URI=https://api.fiskalizacija.hr
FISKALIZACIJA_AUTH_TOKEN=

# Nginx Configuration
NGINX_PORT=3333

# Frontend API Configuration
# Use /api for relative path (recommended - works through nginx proxy)
# Or set full URL if accessing backend directly
REACT_APP_API_URL=/api
